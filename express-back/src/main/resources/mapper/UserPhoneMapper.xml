<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.kuaidi.mapper.UserPhoneMapper">

    <select id="findByStaffId" resultType="com.example.kuaidi.entity.UserPhone">
        SELECT * FROM user_phone WHERE staff_id = #{staffId} ORDER BY create_time DESC
    </select>

    <select id="findByPhone" resultType="com.example.kuaidi.entity.UserPhone">
        SELECT * FROM user_phone WHERE phone = #{phone}
    </select>

    <select id="findById" resultType="com.example.kuaidi.entity.UserPhone">
        SELECT * FROM user_phone WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_phone (staff_id, phone)
        VALUES (#{staffId}, #{phone})
    </insert>

    <delete id="deleteById">
        DELETE FROM user_phone WHERE id = #{id}
    </delete>

    <select id="countByStaffId" resultType="int">
        SELECT COUNT(*) FROM user_phone WHERE staff_id = #{staffId}
    </select>

</mapper>
